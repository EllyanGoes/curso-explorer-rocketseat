function minhaFuncao(meuArray) {
  // Código da função que utiliza o array
  for (let i = 0; i < meuArray.length; i++) {
      console.log(meuArray[i]);
  }
}

// Exemplo de uso
let meuArray = [1, 2, 3, 4, 5];
minhaFuncao(meuArray);
